<div class="trading-places-settings">
    <!-- Header -->
    <div class="settings-header">
        <div class="header-info">
            <h2>Trading Places Settings</h2>
            <div class="version-info">
                <span class="module-version">v{{systemInfo.moduleVersion}}</span>
                <span class="foundry-version">Foundry {{systemInfo.foundryVersion}}</span>
                <span class="system-info">{{systemInfo.activeSystem}}</span>
            </div>
        </div>
        <div class="change-indicator">
            <i class="fas fa-circle"></i>
            <span>Unsaved Changes</span>
        </div>
    </div>

    <!-- Tabs Navigation -->
    <nav class="tabs" data-group="settings-tabs">
        <a class="item active" data-tab="general">
            <i class="fas fa-cog"></i>
            General
        </a>
        <a class="item" data-tab="trading">
            <i class="fas fa-coins"></i>
            Trading
        </a>
        <a class="item" data-tab="ui">
            <i class="fas fa-desktop"></i>
            Interface
        </a>
        <a class="item" data-tab="performance">
            <i class="fas fa-tachometer-alt"></i>
            Performance
        </a>
    </nav>

    <!-- Tab Content -->
    <div class="tab-content">
        <!-- General Tab -->
        <div class="tab" data-tab="general">
            <div class="settings-section">
                <h3><i class="fas fa-info-circle"></i> Module Information</h3>
                <p>Trading Places provides a comprehensive trading system for WFRP4e with dynamic markets and merchant generation.</p>
                <p>Settings are kept minimal by design. Most configuration is handled automatically based on the game world and system.</p>
            </div>
        </div>

        <!-- Trading Tab -->
        <div class="tab" data-tab="trading" style="display: none;">
            <div class="settings-section">
                <h3><i class="fas fa-chart-line"></i> Trading System</h3>
                <p>The trading system operates automatically with realistic economic simulation based on settlement characteristics and seasonal variations.</p>
                <p>Trading behavior is configured through the game world data and doesn't require manual settings.</p>
            </div>
        </div>

        <!-- UI Tab -->
        <div class="tab" data-tab="ui" style="display: none;">
            <div class="settings-section">
                <h3><i class="fas fa-eye"></i> Visual Settings</h3>
                <div class="setting-row">
                    <label for="compactMode">
                        <strong>Compact UI Mode</strong>
                        <span class="hint">Use a more compact layout for smaller screens</span>
                    </label>
                    <input type="checkbox" name="compactMode" {{#if settings.ui.compactMode}}checked{{/if}}>
                </div>
            </div>
        </div>

        <!-- Performance Tab -->
        <div class="tab" data-tab="performance" style="display: none;">
            {{#if isGM}}
            <div class="settings-section">
                <h3><i class="fas fa-database"></i> Data Management</h3>
                <div class="setting-actions">
                    <button type="button" class="export-settings">
                        <i class="fas fa-download"></i>
                        Export Settings
                    </button>
                    <button type="button" class="import-settings">
                        <i class="fas fa-upload"></i>
                        Import Settings
                    </button>
                    <input type="file" class="import-file" accept=".json" style="display: none;">
                </div>
            </div>
            {{/if}}
        </div>
    </div>

    <!-- Footer Actions -->
    <div class="settings-footer">
        <div class="footer-left">
            {{#if isGM}}
            <button type="button" class="reset-settings">
                <i class="fas fa-undo"></i>
                Reset to Defaults
            </button>
            {{/if}}
        </div>
        <div class="footer-right">
            <button type="button" class="cancel-settings">
                <i class="fas fa-times"></i>
                Cancel
            </button>
            <button type="button" class="save-settings" disabled>
                <i class="fas fa-save"></i>
                Save Changes
            </button>
        </div>
    </div>
</div>